{#
  Struct-API Library layout (Hierarchical): the per-block region functions
  (`blk_i_alg` / `blk_i_deriv`), externally linked so `model.c` can call them.
  Includes "blocks.h" (which pulls in "model.h" for `model_t` and the helpers).
-#}
#include "{{ file_base }}_blocks.h"

/* No FMA contraction — round exactly as written, matching the reference runtime
   (see the note in the model TU). */
#pragma STDC FP_CONTRACT OFF

{{ block_fns }}